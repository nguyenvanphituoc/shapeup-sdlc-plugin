shapeup-sdlc-plugin — GATE L2 demo, plain-text transcript
Generated by scripts/demo/record-demo.mjs (npm run demo).

The DENIED block below is verbatim stdout captured from a real run of
hooks/gate-l2.mjs against a real partial board. Every other line is scripted
narration of the surrounding pipeline steps.

------------------------------------------------------------------------
$ claude
> /ship "add dark mode"

  GATE L0   intake + config ................. ok
  GATE L1a  orient (code surface, risk) ..... ok
  GATE L1a.5 wiring map: 4 use cases ........ ok
  GATE L1b  board: 4 tasks .................. ok

  BUILD  TASK-001  tokens ........... ✅ T0 green
  BUILD  TASK-002  toggle-ui ........ ✅ T0 green
  BUILD  TASK-003  persist .......... ⬜ ready
  BUILD  TASK-004  wiring ........... 🔄 in-progress

> looks good — running EVAL now
  (the agent decides it is done. it is not.)

  ⛔ PreToolUse hook — DENIED   Skill(spec-evaluator)
     GATE L2 — board is not green. EVAL runs exactly once per round,
     only after every task is done. Unfinished: TASK-003, TASK-004.
     Route back to BUILD (task-executor) to finish these, then
     re-attempt EVAL. (To override deliberately, the PO can invoke
     spec-evaluator with --task for a single-task check.)

  ↑ verbatim stdout from hooks/gate-l2.mjs.
    Not a prompt. Not a reminder. The tool call
    never reached the evaluator.

  BUILD  TASK-003  persist .......... ✅ T0 green
  BUILD  TASK-004  wiring ........... ✅ T0 green

> EVAL
  ✅ allowed — board green, 4/4
  hill 4/4 · derived from T0 artifacts, not self-report
------------------------------------------------------------------------

Raw hook stdout (partial board):
{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"GATE L2 — board is not green. EVAL runs exactly once per round, only after every task is done. Unfinished: TASK-003, TASK-004. Route back to BUILD (task-executor) to finish these, then re-attempt EVAL. (To override deliberately, the PO can invoke spec-evaluator with --task for a single-task check.)"}}

Raw hook stdout (green board): <empty> — exit 0, i.e. defer, EVAL allowed.
