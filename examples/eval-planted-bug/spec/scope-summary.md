# Scope summary — FizzBuzz CLI

**Pitch:** a one-shot CLI that prints the FizzBuzz sequence to `n`.

## Done when
- `fizzbuzz <n>` prints the correct sequence for every `n ≥ 1`, including the FizzBuzz case
  (multiples of 15) — maps to AC1–AC4.
- Bad input fails gracefully — maps to AC5.

## Non-go
- No flags, no config, no output formats other than newline-separated lines.

## Tasks
- TASK-001 → UC-01 (the whole feature; single task).
