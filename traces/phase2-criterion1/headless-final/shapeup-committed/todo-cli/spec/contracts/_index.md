# Contract Registry — todo-cli

| Repository | Source Type | Service / Engine | Status | SPIKE Task |
|-----------|------------|-----------------|--------|------------|
| [[todo-store.contract]] (TodoStoreRepository) | offline-storage | Node core `fs` (JSON file, `./.todo.json`) | ✅ confirmed | — (de-risked by `.shapeup/todo-cli/orient/spike-persistence.md`, no SPIKE task needed) |
