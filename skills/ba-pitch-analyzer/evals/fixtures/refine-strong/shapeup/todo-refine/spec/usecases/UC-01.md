# UC-01 — Show a done count after the list

**Actor:** CLI user
**Deliverable type:** CLI (non-UI → oracle `process`)

## Steps
1. The user runs the command this use case refines.
2. The existing behaviour runs as before, with the refinement applied.
3. Bad input is refused on stderr with a non-zero exit.

## Invariants
- **INV-01** — `todo list` prints a trailing `2 of 5 done` summary; an empty list still prints only `no todos`.
