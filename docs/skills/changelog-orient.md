# Changelog for orient

| Version | Date | Changes |
|---------|------|---------|
| 1.1 | 2026-08-02 | **ADR-0001 consumer layout** (unreleased). Every path the Scout names moves: the four artifacts go to `.shapeup/<feat>/orient/` and the WorkResult to `.shapeup/<feat>/results/`, while the pitch and spec it reads are under `shapeup/<feat>/`. The old roots (`docs/shapeup-sdlc/`, `.shapeup-sdlc/`) are gone — `docs/` is a namespace many projects publish through a static-site generator, so the spec tree either got published by accident or broke the build. The LOCAL-vs-SHARED distinction the Scout has always observed is unchanged; only the names are, and the orient artifacts stay run-trace (they are inputs to `ba`, not deliverables). Craft untouched: code-surface sweep, single time-boxed spike, discovered-vs-imagined citation rule, hill signal. Earlier (2026-07-24, skills-optimization): the frontmatter `description` folded from a `>` block scalar to one quoted line and shed its pure-worker tail (the four artifacts, the "writes NO production code" list) down to "Also accepts a tech-lead `--order` dispatch" — trigger surface only; those rules live in the body and are unchanged. |
| 1.0 | 2026-07-13 | **Pure-skill alignment** (plan P3): input formalized as a WorkOrder (`--order`) — the output-path convention moves into the order's substrate; the Scout no longer derives pipeline paths itself. Emits a WorkResult envelope (artifacts + deviations) alongside the four domain artifacts. Craft unchanged: code-surface sweep method, single time-boxed spike discipline, discovered-vs-imagined citation rule, hill-signal heuristics. |
| 0.2 | 2026-06-16 | GATE O-A: explicit shaped-pitch signals (frontmatter check). Phase 1: no-breadboard fallback. GATE O-B: SPIKE-NOT-NEEDED path for all-prior-art pitches. Phase 5: Discovered tasks require `file:line` citation. New "Done — completion output" section with ba handoff note (`--orient-dir`). Hard rules updated. |
| 0.1 | 2026-06-10 | Initial Scout. Shape Up step 7 as a pure worker: code-surface map, single time-boxed spike, discovered-task seed, hill signal. Defines the `orient → ba` handoff contract. No code / no board / no run-state / no reporting. |
