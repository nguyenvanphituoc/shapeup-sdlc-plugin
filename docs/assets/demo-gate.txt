shapeup-sdlc-plugin — sandbox-guard demo, plain-text transcript
Generated by tools/demo/record-demo.mjs (npm run demo).

The denial block below is verbatim stdout captured from a real run of
hooks/sandbox-guard.mjs against a real compiled order. Every other line is
scripted narration of the surrounding pipeline steps.

This is a DENY, not a warning: the write does not happen. The other walls are
harness verify envelope, gate-intake, safety-spine and gate-zerowork.

------------------------------------------------------------------------
$ claude
> /ship "add dark mode"

  GATE L0   intake + config ................. ok
  GATE L1a  orient (code surface, risk) ..... ok
  GATE L1a.5 wiring map: 4 use cases ........ ok
  GATE L1b  board: 4 tasks .................. ok

  BUILD  scope tokens   substrate: src/theme/*.ts

> the worker decides billing needs a tweak too
  Edit(src/billing/charge.ts)

  ⛔ PreToolUse hook — sandbox-guard   DENIED
     Sandbox guard (PA3) — no live order's substrate covers these
     writes: src/billing/charge.ts is outside every live order's
     allowed scopes If this write legitimately crosses scopes, the
     order's substrate needs to be expanded (e.g. via ba --remap).

  ↑ verbatim stdout from hooks/sandbox-guard.mjs.
    Not a prompt. A script that read the order's own
    substrate and named the path it does not cover.

> back inside the scope
  Edit(src/theme/tokens.ts) ......... ✅ permitted

  BUILD  scope tokens ............... ✅ T0 green
  hill 4/4 · derived from T0 artifacts, not self-report
------------------------------------------------------------------------

Raw hook stdout (write to src/billing/charge.ts):
{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"Sandbox guard (PA3) — no live order's substrate covers these writes:\nsrc/billing/charge.ts is outside every live order's allowed scopes\nIf this write legitimately crosses scopes, the order's substrate needs to be expanded (e.g. via ba --remap)."}}

Raw hook stdout (write to src/theme/tokens.ts): permitted — no deny payload.
