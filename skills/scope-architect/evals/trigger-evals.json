{
  "skill": "scope-architect",
  "purpose": "Tier-1 trigger eval: does a query activate `scope-architect` (write/remap/split committed scope contracts — vertical slicing, substrates, manifests)? Hardest negatives are `ba-pitch-analyzer` (task decomposition, the step before) and `scope-hammer` (ship-time scope cutting).",
  "cases": [
    { "query": "map the scopes for this feature and write the scope contracts", "should_trigger": true },
    { "query": "remap this scope — the discovered tasks don't fit any substrate", "should_trigger": true },
    { "query": "split the cart-creation scope, it's been stuck uphill for 3 rounds", "should_trigger": true },
    { "query": "write the allowed_file_substrate whitelist for each vertical slice", "should_trigger": true },
    { "query": "derive the affordance manifest and e2e fixtures for these scopes", "should_trigger": true },
    { "query": "break this pitch into a DDD spec tree and task board", "should_trigger": false, "expected_other": "ba-pitch-analyzer" },
    { "query": "we're out of appetite — what do we cut before shipping?", "should_trigger": false, "expected_other": "scope-hammer" },
    { "query": "implement TASK-004 from the spec folder", "should_trigger": false, "expected_other": "task-executor" },
    { "query": "orient on this pitch before we plan anything", "should_trigger": false, "expected_other": "orient" },
    { "query": "rename this variable across the repo", "should_trigger": false, "expected_other": "none" }
  ]
}
